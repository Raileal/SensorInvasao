name: app_flutter
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.6.2

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  
  # Pacote para acessar sensores do dispositivo (acelerômetro, giroscópio)
  proximity_sensor: ^1.3.8

  network_info_plus: ^4.1.0

  camera: ^0.10.5+9


dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
